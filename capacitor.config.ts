import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voycontigo.voycontigo',
  appName: 'VoyContigo',
  webDir: 'dist/frontend-app/browser',
};

export default config;
